version: 1
frontend:
  phases:
    preBuild:
      commands:
        - prebuild.sh
    build:
      commands:
        - build.sh
  artifacts:
    baseDirectory: /public
    files:
      - '**/*'
  cache:
    paths:
      - themes/techblog/node_modules
